"""JitRL Router Web Demo (Session 3 deliverable).

Offline-first, stdlib-only demo server + static frontend:
    python -m demo.server            # offline (default, zero network)
    python -m demo.server --online   # enable the live decision API

See demo/README.md for the full contract.
"""
